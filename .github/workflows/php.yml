name: Build

on: push

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: composer-installer-action
        uses: make install
      - name: PHP Lint
        uses: make lint

